<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ch.alphaCar.mapper.ReservationDao">

	<select id="select" parameterType="string" resultType="reservation">
		select * from reservation where id=#{id} order by rsNo
	</select>
	
	<select id="list" parameterType="reservation"  resultType="reservation">
		<!-- select * from board2 order by num desc -->
		select * from (select a.*, rowNum rn from (
			select * from car 
			<where>
				carRes='n' and carRegion=#{carRegion} and carKind=#{carKind} and carType={carType}
			</where>
			order by carNo)a)
				where rn between #{startRow} and #{endRow}
	</select>
	
	<update id="reserve" parameterType="string">
	  update car set carRes='y' where carNo=#{carNo} between startDate=#{startDate} and endDate=#{endDate}
	</update>
	
	<update id="delete" parameterType="string">
		update car set carRes='n' where carNo=#{carNo}
	</update>
	
	<select id="getTotal" parameterType="reservation" resultType="integer">
		select count(*) from reservation where id=#{id} and del='n'
	</select>
</mapper>